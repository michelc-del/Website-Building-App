import React from 'react';

/**
 * @deprecated This component has been replaced by PageManager.tsx
 * Keeping file to prevent import errors during migration, but it renders nothing.
 */
export const PageSelector: React.FC<any> = () => {
  return null;
};
